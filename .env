# galactic-aggregator-backend/.env
# This file is mostly for local setup or to keep python-dotenv happy.
# Secrets are handled by Render environment variables.
UVICORN_PORT=8001
REFRESH_INTERVAL_SECONDS=30
